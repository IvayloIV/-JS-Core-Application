{{#each planes}}
<a href="#/detailsPlane/{{_id}}" class="added-flight">
    <img src="{{image}}" alt="" class="picture-added-flight">
    <h3>{{destination}}</h3>
    <span>from {{origin}}</span><span>{{date}}</span>
</a>
{{/each}}